name: first workflow

on: [push]

jobs:
    job1:
        runs-on: ubuntu-latest
        steps:
            - name: step1
              run: echo "This is step 1"
            - name: step2
              run: echo "This is step 2"

